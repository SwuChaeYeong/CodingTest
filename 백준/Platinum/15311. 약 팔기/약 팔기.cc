#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    cin >> N; // 입력은 주어지지만 사실 항상 1,000,000

    cout << 2000 << "\n";

    // 1을 1000개
    for (int i = 0; i < 1000; ++i)
        cout << 1 << " ";

    // 1000을 1000개
    for (int i = 0; i < 1000; ++i)
        cout << 1000 << " ";

    return 0;
}